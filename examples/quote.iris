; quote is built-in macro that returns single parameter as it is
(echo (quote (1 2 3 4)))

; echo that is encapsulated will not be called
(echo (quote echo))

; default reader supports ' marker that forces next object to be quoted
'(1 2 3)
