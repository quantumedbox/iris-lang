(echo (quote! (1 2 3 4))) ; quote is built-in macro that returns single parameter as it is

(echo (quote! echo)) ; quoted symbols aren't resolved to functions
(echo echo)          ; compare to this

(echo '(4 3 2 1))    ; default reader supports ' marker that forces next object to be quoted
